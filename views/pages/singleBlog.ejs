<!DOCTYPE html>
<html lang="en">

<%- include('../partials/header.ejs') %>
<%- include('../partials/nav.ejs') %>

    <body class="container">
        <div class="center mb-mid">
            <h1 class="max-75">
                <%= blog.title %>
            </h1>
            <img src="../../img<%= blog.url %>" alt="">
            <div>
                <%= blog.author %>
            </div>
            <div>
                <img src="<%= blog.author_bild %>" alt="author">
            </div>
            <div>
                <%= blog.published_at %>
                    <%= blog.duration %>
            </div>
            <div>
                <%= blog.body %>
            </div>
        </div>
    </body>

</html>